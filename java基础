## 为什么java 中很多地方在接口类中定义接口，这样做有什么好处
  ① 逻辑分组与命名空间管理
  - 所有与 ChatClient 相关的操作（请求构建、同步响应、流式响应）都被封装在 ChatClient 这个顶级接口内部，形成了清晰的逻辑单元。
避免了在全局命名空间中定义大量独立的接口（如 ChatClientRequest、ChatClientResponse），减少了命名冲突的可能，也让代码结构更紧凑。
  - 
